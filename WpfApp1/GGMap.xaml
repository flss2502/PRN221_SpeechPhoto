<Window x:Class="SpeechPhoto_WPF.GGMap"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpeechPhoto_WPF"
                xmlns:gmaps="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
        mc:Ignorable="d"
        Title="GGMap" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border x:Name="mapContainer" Width="780" Height="333" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">
            <gmaps:GMapControl x:Name="mapView" Loaded="mapView_Loaded" Margin="0,10,0,0"/>
        </Border>

        <StackPanel VerticalAlignment="Top" Margin="0,366,0,0">
            <TextBox x:Name="latitudeTextBox" Margin="0,0,0,5" Text="Latitude" />
            <TextBox x:Name="longitudeTextBox" Margin="0,0,0,5" Text="Longitude" />
            <Button Content="Move" Click="MoveButton_Click" />
            <Button Content="Move to API Location" Click="MoveToAPILocation_Click" />
        </StackPanel>
    </Grid>
</Window>
